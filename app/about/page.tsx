export default function About() {
  return (
    <div>
      <h2>About</h2>
      <p>
        This Person App uses Auth.js (NextAuth v5) with Google OAuth for authentication. The architecture uses a Next.js App Router with server and API routes protected by session checks.
      </p>
    </div>
  )
}
