// Prisma removed — project migrated to MongoDB (Mongoose).
// This file is intentionally left as a placeholder to avoid import errors in older references.
// Use `lib/mongodb.ts` and the exported `connect`, `User`, and `Person` models instead.
export default null
